// Test that calling a rust fn in interpreter gives correct error

error NotFound = "File not found"

rust fn read_file(path: str) -> str!

fn main() {
    print "Testing interpreter rejection of rust fn calls..."
    
    // This SHOULD fail with a specific error message
    var content = read_file("test.txt")
    
    print "ERROR: Should not reach here!"
}

main()
