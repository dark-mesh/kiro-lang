pure fn add(a: num, b: num) -> num {
    return a + b
}

fn main() {
    print "Start"
    
    // 1. Sync Call from Async context
    var res = add(10, 20)
    print res
    
    // 2. Async wrapper (Run) of pure function
    run add(30, 40)
    
    print "Done"
}
main()
